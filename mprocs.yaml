procs:
  postgres:
    shell: >
      docker run --rm --name dev
      -e POSTGRES_USER=dev
      -e POSTGRES_PASSWORD=dev
      -e POSTGRES_DB=dev
      -p 5432:5432
      postgres:16
  web:
    shell: >
      dotnet run
